{
  "name": "isca-digital-root",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "bcrypt": "^5.1.0",
    "jsonwebtoken": "^9.0.0",
    "uuid": "^9.0.0"
  },
  "scripts": {
    "vercel-build": "npm --prefix client install && npm --prefix client run build"
  }
}
